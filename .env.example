# ===== Backend App =====
# Connection settings for the Go application
DB_HOST=db
DB_PORT=3306
DB_USER=nimuser
DB_PASSWORD= # Should be filled in .env
DB_NAME=nim
DB_PARAMS=parseTime=true&charset=utf8mb4&collation=utf8mb4_unicode_ci

# ===== Docker Compose (MySQL) =====
# These variables are used by the official MySQL image to initialize the database
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=nim
MYSQL_USER=nimuser
MYSQL_PASSWORD=

# ===== Dev helpers =====
# URL for the frontend development server (for CORS configuration on backend)
FRONTEND_URL=http://localhost:5173
# Internal URL for Nginx to proxy requests to the backend container
BACKEND_URL=http://backend:8080